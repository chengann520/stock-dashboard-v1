name: Daily AI Trading Bot

on:
  schedule:
    - cron: '0 21 * * 0-4' 
    - cron: '30 7 * * 1-5'
  workflow_dispatch:
    inputs:
      action:
        description: '執行動作 (predict 或 settle)'
        required: true
        default: 'predict'

# 👇 修改重點 1：把 env 移到最外面，這樣整個檔案裡的所有步驟都能用
env:
  SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
  SUPABASE_KEY: ${{ secrets.SUPABASE_KEY }}
  FINMIND_TOKEN: ${{ secrets.FINMIND_TOKEN }}
  DATABASE_URL: ${{ secrets.DATABASE_URL }}

jobs:
  trade:
    runs-on: ubuntu-latest
    
    steps:
    - name: 下載程式碼
      uses: actions/checkout@v3

    - name: 設定 Python 環境
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"

    - name: 安裝套件
      run: |
        # 👇 補上 tqdm 與 pandas_ta 即可
        pip install supabase finmind pandas plotly tqdm pandas_ta

    # 👇 修改重點 2：我加了一個「偵探步驟」，幫您檢查變數到底有沒有進來
    - name: Debug - 檢查金鑰狀態
      run: |
        echo "正在檢查金鑰..."
        if [ -z "$SUPABASE_URL" ]; then
          echo "❌ 慘了... Linux 系統說 SUPABASE_URL 是空的"
        else
          echo "✅ 成功！Linux 系統讀到了 SUPABASE_URL (長度: ${#SUPABASE_URL})"
        fi

    - name: 判斷執行模式
      id: check_schedule
      run: |
        current_hour=$(date +%H)
        # UTC 21:00 (TWT 05:00) 判定為預測，UTC 07:30 (TWT 15:30) 判定為結算
        if [ "$current_hour" -gt "12" ]; then
          echo "action=predict" >> $GITHUB_OUTPUT
        else
          echo "action=settle" >> $GITHUB_OUTPUT
        fi
      
    - name: 執行 AI 交易機器人
      # 👇 因為上面已經設定了全域 env，這裡不需要再寫一次 env
      run: |
        if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
           python auto_trade.py --action ${{ inputs.action }}
        else
           python auto_trade.py --action ${{ steps.check_schedule.outputs.action }}
        fi
